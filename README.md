# Inventory System API

## Description
A Flask-based inventory system with CLI support and external product lookup using barcode API (OpenFoodFacts).

## Features
- Add / update / delete inventory items
- View inventory via API and CLI
- Fetch product by barcode
- Persistent JSON storage
- Automated tests with pytest

## Setup

```bash
pip install -r requirements.txt